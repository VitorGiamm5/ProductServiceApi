# Conexões locais
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust

# Permite conexões da rede interna do Docker
host    all             all             0.0.0.0/0               md5

# Permite replicação
host    replication     repl_user       0.0.0.0/0               md5